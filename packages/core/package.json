{
  "name": "@commit-rag/core",
  "version": "0.0.0",
  "private": true,
  "description": "IDE-independent core engine for commit-rag: git, indexing, retrieval, prompt, LLM",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.5.0"
  }
}
